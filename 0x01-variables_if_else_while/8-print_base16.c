#include <stdio.h>

/**
 * main - Lazador
 *
 * Return: Always 0 
 */

int main(void)
{
	char a = "0';
	char l = 'a';

	for (a = '0'; a <= '0'; a++)
	{
		putchar (a);
	}
	for (l = 'a'; l <= 'a'; l++)
	{
		putchar (l)
	}
	putchar ('\n');
	return (0);
}
